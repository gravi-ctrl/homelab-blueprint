services:
  robot:
    build: .
    container_name: wifi-robot
    init: true
    restart: unless-stopped
    ports:
      - "5050:5000"
    environment:
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
