# ==========================================
# WHAT IS THIS
# ==========================================
# This file triggers a warning when you cd into this folder (once per ssh session).
# Any line starting with a # will be hidden.
# Anything else below will be shown as a red warning.
# The main lines are added in ~/.zshrc
# ==========================================

Do not commit any secrets here that aren't gitignored. This dir gets auto-committed daily at 5am.
